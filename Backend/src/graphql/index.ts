import submissionResolvers from "./submission/resolvers";
import submissionTypeDefs from "./submission/schema";

export const typeDefs = [submissionTypeDefs];

export const resolvers = [submissionResolvers];
